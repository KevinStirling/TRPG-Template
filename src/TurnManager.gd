extends Resource
class_name TurnManager

#	look into making this more type safe, make sure dirctionary has units only
func _init(_units : Dictionary, _enemy_units : Dictionary) -> void :
	print(_units)
	print(_enemy_units)

